export function Red({ text, className }) {
    return (
        <p className={` ${className} px-1 tred`}>{text}</p>
    )
}
export function Green({ text, className }) {
    return (
        <p className={` ${className} px-1 tgreen`}>{text}</p>
    )
}
export function Pink({ text, className }) {
    return (
        <p className={` ${className} px-1 tpink`}>{text}</p>
    )
}
export function Blue({ text, className }) {
    return (
        <p className={` ${className} px-1 text-sky-500`}>{text}</p>
    )
}
export function Yellow({ text, className }) {
    return (
        <p className={` ${className} px-1 tyellow`}>{text}</p>
    )
}
export function Purple({ text, className }) {
    return (
        <p className={` ${className} px-1 tpurple`}>{text}</p>
    )
}
export function Orange({ text, className }) {
    return (
        <p className={` ${className} px-1 torange`}>{text}</p>
    )
}

